<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="me.majiajie.android_barcode_reader">

    <application
        android:allowBackup="true"
        android:name=".APP"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".QRCodeGenerateActivity"
            android:label="QR Code Generate"/>

        <activity android:name=".BarCodeGenerateActivity"
            android:label="Bar Code Generate"/>

        <activity android:name=".CustomScanActivity"
            android:label=""
            android:theme="@style/TranslucentStatus"
            android:screenOrientation="portrait"
            android:configChanges="orientation|keyboard|keyboardHidden"/>

    </application>

</manifest>